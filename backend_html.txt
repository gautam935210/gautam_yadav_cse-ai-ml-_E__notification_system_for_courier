<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parcel Status Update | Mamta Courier</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        :root {
            --primary: #2c3e50;
            --secondary: #e74c3c;
            --accent: #3498db;
            --gold: #f1c40f;
            --light: #ecf0f1;
            --dark: #2c3e50;
            --success: #27ae60;
            --warning: #e67e22;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #f9f9f9 0%, #e4e4e4 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            color: var(--dark);
        }

        .container {
            width: 100%;
            max-width: 550px;
            animation: fadeIn 0.8s ease-out;
        }

        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        .company-name {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 5px;
            position: relative;
            display: inline-block;
        }

        .company-name::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(to right, var(--gold), var(--secondary));
            border-radius: 3px;
        }

        .tagline {
            font-style: italic;
            color: #7f8c8d;
            font-size: 0.9rem;
            letter-spacing: 1px;
        }

        .card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            padding: 40px;
            position: relative;
            z-index: 1;
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 8px;
            background: linear-gradient(to right, var(--secondary), var(--gold));
        }

        h1 {
            color: var(--primary);
            margin-bottom: 30px;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            font-size: 1.8rem;
            text-align: center;
        }

        .form-group {
            margin-bottom: 25px;
            position: relative;
        }

        .form-group label {
            display: block;
            margin-bottom: 10px;
            font-weight: 500;
            color: var(--dark);
            font-size: 0.95rem;
        }

        .form-control {
            width: 100%;
            padding: 15px 20px;
            border: 2px solid #e9ecef;
            border-radius: 10px;
            font-size: 16px;
            transition: all 0.3s;
            background-color: #f8f9fa;
            font-family: 'Montserrat', sans-serif;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--accent);
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.2);
            background-color: white;
        }

        .btn {
            background: linear-gradient(to right, var(--secondary), var(--primary));
            color: white;
            border: none;
            padding: 16px 30px;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            width: 100%;
            margin-top: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            letter-spacing: 0.5px;
            text-transform: uppercase;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(231, 76, 60, 0.3);
        }

        .btn:active {
            transform: translateY(0);
        }

        .footer {
            margin-top: 30px;
            text-align: center;
            color: #7f8c8d;
            font-size: 0.85rem;
        }

        .copyright {
            margin-top: 10px;
            font-size: 0.8rem;
        }

        .parcel-icon {
            font-size: 32px;
            color: var(--secondary);
        }

        .status-options {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: 10px;
        }

        .status-tag {
            padding: 8px 16px;
            background-color: #e9ecef;
            border-radius: 20px;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.2s;
            font-weight: 500;
        }

        .status-tag:hover {
            background-color: #dee2e6;
        }

        .status-tag.active {
            background-color: var(--secondary);
            color: white;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 576px) {
            .card {
                padding: 30px 20px;
            }

            .company-name {
                font-size: 2rem;
            }

            h1 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="company-name">Mamta Courier</div>
            <div class="tagline">Excellence in Logistics and Beyond</div>
        </div>
        <div class="card">
            <h1>
                <i class="fas fa-box-open parcel-icon"></i>
                Parcel Status Update
            </h1>
            <form id="updateForm">
                <div class="form-group">
                    <label for="tracking_id">Tracking ID</label>
                    <input type="text" id="tracking_id" name="tracking_id" class="form-control" placeholder="Enter your tracking number" required>
                </div>
                <div class="form-group">
                    <label for="status">New Status</label>
                    <input type="text" id="status" name="status" class="form-control" placeholder="Enter new status" required>
                    <div class="status-options">
                        <div class="status-tag" data-status="In Transit">In Transit</div>
                        <div class="status-tag" data-status="Out for Delivery">Out for Delivery</div>
                        <div class="status-tag" data-status="Delivered">Delivered</div>
                        <div class="status-tag" data-status="On Hold">On Hold</div>
                        <div class="status-tag" data-status="Returned">Returned</div>
                    </div>
                </div>
                <button type="submit" class="btn">
                    <i class="fas fa-paper-plane"></i>
                    Update Status
                </button>
            </form>
        </div>
        <div class="footer">
            <p>Mamta Courier - Customer Notification System</p>
            <p class="copyright">© 2023 Mamta Courier. All rights reserved.</p>
        </div>
    </div>
    <script>
        const form = document.getElementById('updateForm');
        const statusTags = document.querySelectorAll('.status-tag');
        const statusInput = document.getElementById('status');

        // Add click event to status tags
        statusTags.forEach(tag => {
            tag.addEventListener('click', () => {
                statusTags.forEach(t => t.classList.remove('active'));
                tag.classList.add('active');
                statusInput.value = tag.getAttribute('data-status');
            });
        });

        form.addEventListener('submit', async function (event) {
            event.preventDefault();
            const tracking_id = document.getElementById('tracking_id').value;
            const status = document.getElementById('status').value;

            // Show loading state
            const btn = form.querySelector('button');
            const originalBtnText = btn.innerHTML;
            btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Processing...';
            btn.disabled = true;

            try {
                const response = await fetch('http://127.0.0.1:5000/update-status', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ tracking_id, status }),
                });

                const result = await response.json();

                // Show success/error message
                if (response.ok) {
                    showAlert('success', result.message || 'Status updated successfully!');
                    form.reset();
                    statusTags.forEach(t => t.classList.remove('active'));
                } else {
                    showAlert('error', result.error || 'Failed to update status');
                }
            } catch (error) {
                showAlert('error', 'Network error. Please try again.');
            }
